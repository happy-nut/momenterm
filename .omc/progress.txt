# Ralph 진행 로그 — 계획 완수 루프

## 컨텍스트
- xctest 없음 → 모든 회귀 테스트는 swiftc 격리 스모크(scripts/*-smoke.sh).
- 매 스토리 검증: ./scripts/build.sh exit 0 + 전체 스모크 green.
- 이미 완료(Ralph 이전): God object 리팩토링 #1~#4 + Step A/B파일럿, 화살표/패딩/드래그/좀비detach 버그, cmux 축1(알림 1a~1d).
- 스모크 현황(10종): smoke, theme, ansi, renderer, textviews, merged-prompt, terminal-view, agent-notification, agent-alert-nav, pty-detach.

## 학습된 코드베이스 패턴
- 뷰/상태 결합 로직 추출: 순수 부분을 별도 enum/struct로 빼서 격리 스모크(MergedPromptNavigation, AgentAlertNavigation, AgentNotification 패턴).
- 큰 블록 이동: awk로 정확히 옮기고 seam 확인 + 백업(scratchpad).
- 공유 뷰(old/newTextView)는 CodePaneController가 소유, 도메인은 API 경유(Step B).
- 알림: processTerminalOutput → handleAgentNotification(bell+OSC 공통) → workspace dot + macOS 알림 + pane 링.
- tmux persistence: MOMENTERM_ENABLE_TMUX_PERSISTENCE=1 + tmux 설치. detach=프로세스 유지, kill=kill-session.

## 스토리 진행
- US-1 (좀비 완결) ✅ AppDelegate.applicationWillTerminate → MainWindowController.detachTerminalSessionsForQuit → ptyManager.detachAll(프로세스 유지). deinit 미호출(NSApplication.terminate=exit) 대비. killAll 미사용 확인. 빌드+스모크10 green.
- US-2 (시계 RPROMPT) 착수 — 수정지점: NativeAnsiRenderer.moveCursorColumnClippingOverflow clamp + MainWindowController.terminalVisibleBounds/syncTerminalSize 분할 시 layout→columns 재계산 순서.
- US-7 추가: frontend-design 기반 디자인 시스템 + UI 개편 (사용자 요청, 우선순위 7).
